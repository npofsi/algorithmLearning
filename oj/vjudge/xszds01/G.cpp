#include<iostream>
#include<string>
#include<math.h>
#include<string.h>
#include<vector>
#include<algorithm>
#include<queue>
using namespace std;


typedef struct Comb{
	vector<Comb> near=vector<Comb>(6);
	int state=1;
} Comb;

bool isSurrounded(Comb cb) {
	for (int i = 0; i < 6; i++) {
		if(cb.near[])
	}
}

int main() {

	return 0;
}