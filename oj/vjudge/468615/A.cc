#include <iostream>
#include <vector>
#include <cstdio>
#define ll long long int
using namespace std;
ll arr[10000];
int main()
{
    ll N, R;

    scanf("%lld %lld", &N, &R);
    for (ll i = 0; i < N; i++)
    {
        ll p;
        scanf("%lld", &p);

        ll n = R - p;
        }
    return 0;
}